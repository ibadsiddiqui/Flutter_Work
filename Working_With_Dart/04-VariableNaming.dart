main(List<String> args) {
  var name1 = 'Bob';
  int lineCount;
  assert(lineCount == null);

  // using Final keyword or const non-changeable values
  final name2 = 'Bob'; // Without a type annotation
  final String nickname3 = 'Bobby';

}
